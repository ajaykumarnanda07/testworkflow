name: firstaction Workflow
on: push
jobs:
  first-job:
    runs-on: test-host
    if: ${{ vars.BUILD_STATUS == 'true' }}
    steps:
      - name: Print greeting
        run: echo "Hello-hi"
      - name: Print goodbye
        run: echo "bye"
      - name: Run Powershell script
        shell: pwsh
        run: |
           $mysecretvalue = $env.GITHUB_NEXUS_USERNAME
           Write-Host "My Secret username is $mysecretvalue"
