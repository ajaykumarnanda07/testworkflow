name: firstaction Workflow
on: workflow_dispatch
jobs:
  if: env.BUILD_STATUS == 'true'
  first-job:
    runs-on: test-host
    steps:
      - name: Print greeting
        run: echo "Hello"
      - name: Print goodbye
        run: echo "bye"
